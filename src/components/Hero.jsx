import { ComputersCanvas } from "./canvas";
import "./Hero.css";

const Hero = () => {
  return (
    <section className={`relative h-screen mx-auto mt-[-100px] adjust-width`}>

      <ComputersCanvas />

    </section>
  );
};

export default Hero;
